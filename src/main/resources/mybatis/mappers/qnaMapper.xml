<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="qnaMapper">
  
  
  <select id="qnaCount" resultType="java.lang.Integer">
  		select Count(num) from qna  
  </select>
  
  <select id="qnaList" parameterType="com.jy.s3.util.Pager" resultType="com.jy.s3.model.board.QnaVO">
  		select * from
  		(select rownum R, Q.* from				<!--rownum 추가 및 q 테이블에 있는 모든것-->
  		(select * from qna order by num desc) Q) <!--Q라는 테이블로 만듬(서브쿼리)-->
  		where R between #{startRow} and #{lastRow}
  </select>
  
  
  
  
  
  
  
  
  
  
  </mapper>
